# GENERATED by scripts/derive_cases.py from the tested snippet
# corpus; regenerate after snippets.yaml changes. Requests are
# the notebooks' own intro prose, expectations are the snippet
# records. Do not hand-edit.
cases:
- id: nb-oscillator-single-job-v1
  request: One calculation, every dapi step. A 5%-damped oscillator is shaken
    exactly at its natural frequency, and one job on Stampede3 integrates the
    motion and reports the dynamic amplification, which lands at the top of the
    resonance curve, near $1/(2\xi) = 10$. The oscillator obeys $$\ddot{x} + 2\xi\omega_n\dot{x}
    + \omega_n^2 x = \frac{F_0}{m}\sin(\Omega t)$$ and this notebook computes
    the single point at $\Omega = \omega_n$. The PyLauncher sweep fills in the
    rest of the curve with one task per frequency; here the point is the dapi
    lifecycle itself, from a folder on disk to a result file in the arc
  expect:
    app_id: python-s3
  derived_from: dapi/examples/python/first-dapi-job.ipynb
- id: nb-resonance-pylauncher-sweep-v1
  request: A damped oscillator answers an earthquake-engineering question with
    one number. Shake it at a given frequency and, once the transient dies away,
    the steady response settles at some multiple of the static displacement, the
    **dynamic amplification**. Repeat that at many frequencies and the numbers
    trace the resonance curve, small away from the natural frequency and peaked
    near it. Each frequency is one independent simulation, which is exactly the
    shape PyLauncher runs well. One Tapis job gets a node, and PyLauncher feeds
    the frequency list to its cores until the sweep is done. This notebook buil
  expect:
    app_id: python-s3
  derived_from: dapi/examples/pylauncher/pylauncher_sweep.ipynb
- id: nb-pm4sand-freefield-v1
  request: A layered liquefiable soil profile sits on an elastic half-space and
    an earthquake motion arrives from below. The OpenSees model uses SSPquadUP
    elements for coupled soil and pore water, the PM4Sand constitutive model (Boulanger
    and Ziotopoulou 2017) for the liquefiable layers, and a viscous damper at
    the base for the half-space's finite rigidity. <img src="schematic.png" width="200"
    align="center">
  expect:
    app_id: opensees-s3
    main_program: OpenSees
  derived_from: dapi/examples/opensees/pm4sand/DS_PM4Sand_FreeField_Solution.ipynb
- id: nb-openseesmp-multimotion-v1
  request: This example shows how to run OpenSeesMP in DesignSafe from a jupyter
    notebook using the DesignSafe API (dapi). A set of four 1D profiles is analyzed
    using OpenSeesMP.
  expect:
    app_id: opensees-mp-s3
  derived_from: dapi/examples/opensees/OpenSeesMP-dapi.ipynb
- id: nb-opensees-ml-dag-v1
  request: 'The OpenSees ML example runs its whole pipeline inside one `python-s3`
    job. This notebook runs the same study as an explicit **directed acyclic graph**:
    the 75-run sweep and the training are separate Tapis jobs that run **one after
    another**, connected by the sweep''s output. ``` sweep (48-core PyLauncher
    job) ──archive_uri──> train (1-core job) ``` Splitting the pipeline lets a
    retrain cost one core instead of a 48-core resweep, and lets each stage size
    its own resources. `run()` hands the graph to DesignSafe''s workflow service,
    which submits each job on your behalf the moment its dependencies'
  expect:
    app_id: python-s3
  derived_from: dapi/examples/opensees_ml/DS_OpenSees_ML_Workflow_DAG.ipynb
- id: nb-pi-fanout-workflow-v1
  request: Three Monte-Carlo jobs estimate pi from different random seeds at the
    same time, and a fourth job combines their counts. This is the fan-out/fan-in
    shape behind many real studies, independent pieces first, one combining step
    after, and pi makes it self-checking, since the combined estimate must land
    near 3.14159. ``` pi-a ──┐ pi-b ──┼──> aggregate pi-c ──┘ ``` DesignSafe's
    workflow service runs the graph. It submits all three shards on your behalf
    at once, holds the aggregator until every shard has archived, and keeps running
    if you close this notebook. The OpenSees ML workflow shows the seque
  expect:
    app_id: python-s3
  derived_from: dapi/examples/workflows/pi_fanout/DS_Pi_Fanout_Workflow.ipynb
- id: nb-quofem-sensitivity-v1
  request: 'This example shows how to run a quoFEM global sensitivity analysis
    on DesignSafe from a Jupyter notebook using the DesignSafe API (dapi), and
    how to postprocess the results in Python. The analysis performs Monte Carlo
    sampling (500 samples) of three PM4Sand constitutive model parameters (`Dr`,
    `G0`, `hpo`) in a cyclic direct simple shear simulation and reports the sensitivity
    of the number-of-cycles engineering demand parameters (EDPs). **Note: This
    example takes roughly 20 minutes end-to-end on Stampede3 (`skx-dev` queue).**
    **Note: Executing this notebook requires a TACC allocation, set in `'
  expect:
    app_id: simcenter-uq-stampede3
  derived_from: dapi/examples/quoFEM-sensitivity/quoFEM-sensitivity-dapi.ipynb
